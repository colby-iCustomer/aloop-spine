# 03 - UI Components

## What Docs Say

### Shared Components (`components/common/`)

**StatusBadge** - Single source of truth for all status display. Never use raw `<Badge>` for status values.
- Supported statuses: `active`, `ready`, `completed`, `connected`, `inactive`, `disconnected`, `pending`, `processing`, `paused`, `running`, `syncing`, `building`, `error`, `failed`
- Unknown statuses fall back to `inactive`
- Input is `.toLowerCase()`-normalized
- Covers `AudienceStatus`, `EntityStatus`, `Campaign.status`, `Transaction.status`, `Dataset.status`

**PageHeader** - Shared page title/description/actions pattern. Used on every page.

**EmptyState** - Empty state display with optional icon, title, description, and action. Accepts `className`.

**InfinityLoader** - Fullscreen/overlay loading indicator. Uses `react-loader-spinner` InfinitySpin. Default width=200, color="#2966BC". Used for non-table loading (workspace switching, page transitions, onboarding).

### Card Family System (4 families)

| Family | Use | Key Variants |
|--------|-----|-------------|
| 1 - Action Card | Navigable/clickable cards | Full (Quick Actions), Compact (New List From), Data (Recently Used), Info (Pixel features) |
| 2 - Stat Card | Metric display (icon + title + number) | Featured (24px number), Compact (22px number) |
| 3 - Connect Card | Integration cards with CTA button | 44px icon, 1-line title, 2-line desc, pill + button footer |
| 4 - Gallery Card | Template cards (unique) | rounded-3xl, decorative circles, field tags |

**Shadow Rules:**
- Clickable card: `shadow-sm` at rest + larger shadow on hover
- Non-clickable card: no shadow at rest or hover
- Shadow communicates interactivity

**Border Rules:**
- All borders 1px, never 2px
- Full Action Cards on gradient bg: transparent at rest, colored on hover
- Compact Action Cards on white bg: 40% opacity colored border at rest, 100% on hover
- Data variant and all other cards: standard `border-border`

### Grid Components (`features/grid/`)

- `GridToolbar` - toolbar above the grid
- `EnrichmentDropdown` - scope selection for enrichment
- `EnrichmentConfirmDialog` - credit confirmation before enrichment
- `EnrichmentProgressBar` - progress during active enrichment
- `EnrichableHeader` - column header with enrichment indicators
- `AddColumnButton` - add new column
- `AddRowsFooter` - add rows at bottom of grid
- `ActionsPanel` - side panel for export actions
- `EnrichmentCellRenderer` - custom cell renderer reading from singleton tracker

### Template Components (`features/templates/`)

- `TemplateCard` - gallery card for template selection
- `TemplatePreviewModal` - detailed template preview with CTA buttons
- `ListPickerModal` - folder/list browser for "Use my List" flow
- `TemplateProgressOverlay` - 4-step progress (Select, Creating List, Auto-Mapping, Loading Grid)
- `TemplateMappingPanel` - side panel for manual mapping adjustments

### Notification Components (`features/notifications/`)

- `NotificationBell` - bell icon with badge count + popover trigger
- `NotificationPopover` - popover with enrichment progress cards + Novu notifications
- `NovuNotificationItem` - single Novu notification display
- `NovuProviderWrapper` - wraps app tree in Novu provider

### Layout Components

- `AppLayout` - app shell wrapper
- `AppSidebar` - collapsible sidebar with workspace switcher, navigation, folder tree
- `TopBar` - header bar with search, notifications, user profile
- `CreditsPopover` - credit balance display

### Settings Components

- `WorkspaceTab` - workspace details + team management
- `TeamManagementTab` - member list, role changes, invitations
- `BillingPlansTab` - plan comparison and upgrade
- `UsageTab` - credit usage display
- `APIKeysTab` - personal and team API key management
- `IntegrationsTab` - connected accounts management

### Auth Components

- `GoogleOAuthButton` - Google native sign-in with business email domain blocking
- `ProtectedRoute` - auth guard for routes

### Workspace Components

- `WorkspaceSelector` - controlled dropdown for workspace switching (splits owned vs shared, role badges)

### Loading Patterns

Two distinct patterns:
1. **Data Skeleton** - shown while API data loads. Skeleton IS the loading state. Renders shimmer cards matching expected layout. Unmounts when data arrives.
2. **Embed Overlay Skeleton** - cosmetic cover over third-party embeds (iframes). Real content mounted underneath. Fades out after iframe `onLoad`.

### PLG Utility Classes

| Class | Use |
|-------|-----|
| `.plg-card` | Standard card with hover shadow |
| `.status-dot` | Tiny inline status indicator dot |
| `.data-label` | Label above form fields |
| `.mono-value` | Monospace display for IDs, API keys, codes |

### Pill Pattern - Static Data Display

| Data Type | Color |
|-----------|-------|
| Record counts | Green (`bg-green-100 text-green-800`) |
| Account counts, tags | Blue (`bg-primary/10 text-primary`) |
| Empty/disconnected | Amber (`bg-amber-100 text-amber-800`) |


## Confidence
**High** - component families, shared components, and card system are well-documented in CLAUDE.md and styling decisions. Feature modules documented in decision journal entries.


## Sources
- `CLAUDE.md` (root) - shared components, PLG utility classes, feature-based architecture
- `docs/current-repo/working-docs/styling-decisions.md` - card family system, skeleton patterns, pill patterns
- `docs/current-repo/flows/template-cta-workflows.md` - template component relationships
- `docs/current-repo/flows/ag-grid-rendering.md` - grid component architecture
- `docs/old-repo-retro/workflows/notifications-journey.md` - notification component architecture
- `docs/current-repo/decisions/README.md` - completion journals for each feature module


## Gaps
- No comprehensive component inventory with props documentation
- No documentation of the `components/folder/` modal components (CSV upload, Google Sheets, HubSpot, Webhooks) beyond mentioning they exist
- No documentation of `components/list/` modal components
- No documentation of form components or form patterns
- No accessibility documentation for any components
- Style guide page (`/dev/style-guide`) is documented as existing but its actual rendered content is not documented


## Regression Flags
- Old repo had **309 components across 37 directories**. New repo organizes into feature modules. Verify component count coverage.
- Old repo had **35 grid components** (8,499 lines) and **22 export components** (15,646 lines). These were identified as heavily duplicated. Verify deduplication in new repo.
- Old repo had `CreditWarningDialog` AND `EnrichmentConfirmationDialog` as two overlapping credit-check dialogs. Verify consolidated.
- Old repo had separate analytics page components per platform (LinkedIn, Meta, SmartLeads) with 80%+ shared logic. Verify parameterized in new repo or still duplicated.
- Old repo had `UpdateColumnPromptPopover` (815 lines) mixing AI prompt + formula features. Verify decomposed.
- Old repo had `GoogleSheetsExportModal` (1,591 lines) combining OAuth + sheet selection + sync. Verify decomposed.
- Old repo had `HybridNotifications` (1,369 lines) as a monolith. New repo docs describe decomposed notification feature module.
